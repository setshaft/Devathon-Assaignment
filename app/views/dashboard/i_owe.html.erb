
<%= render 'form'  %>
<div id="page-wrapper">
	<div class="container-fluid">
		<div class="col-xs-6 col-sm-4">
			<% @expense.each do |money|%>
			You owe <b> <%= money.amount%></b>  rupees to <b><%= money.owe_to %></b> 
			<br>

			<% end %>

		</div>
	</div>
</div>
</div>
